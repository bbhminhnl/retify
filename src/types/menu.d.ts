interface MenuData {
  name_vi: string;
  name_en: string;
  price: string;
  description: string;
  image_prompt: string;
  image_url?: string; // Thêm trường mới cho ảnh DALL-E
  name: string; // Tên món ăn
  unit: string; // Đơn vị tiền tệ
}
